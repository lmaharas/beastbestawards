{#items} 
<li class="bubble {categoryId}">
    <a class="circle" href="#{id}">
        <img src="{imgPath}">
        <h3 class="title">{?tweet}{handle}{:else}{name}{/tweet}</h3>
    </a>
</li>
{/items}